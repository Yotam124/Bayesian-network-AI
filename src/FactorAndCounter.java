public class FactorAndCounter {
    Factor factor;
    int sumCounter = 0;
    int computeCounter = 0;

    FactorAndCounter(Factor factor, int sumCounter, int computeCounter) {
        this.factor = factor;
        this.sumCounter = sumCounter;
        this.computeCounter = computeCounter;
    }
}
